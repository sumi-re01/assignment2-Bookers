<!--バリデーションエラー-->
<% if model.errors.any? %>
  <div id="error-text">
    <h3>
      <%= pluralize(model.errors.count, "error") %> prohibited this book from being saved:
    </h3>

    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>